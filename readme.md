# finance-bot

TO-DO: write everything properly

Temporary know-how:

needs these env vars:
- WEBHOOK_URL
- SPREADSHEET_ID
- GOOGLE_APPLICATION_CREDENTIALS -  path where the service account json is mounted inside the container, e.g. /secrets/service-account.json

